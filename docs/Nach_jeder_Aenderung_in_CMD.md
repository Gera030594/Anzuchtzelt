# Archiviert: Nach jeder Aenderung in CMD

Status: archiviert. Aktueller Ablauf siehe `../05_Entwicklung.md`.

Alter Inhalt:

```text
- Auswaehlen
cd C:\Users\hraki\OneDrive\Anzucht\Hauptprogramme\Hauptprogramm_V17

- Nach jeder Aenderung
git status
git add .
git commit -m "Beschreibung der Aenderung"
git push

- Beispiel
git status
git add .
git commit -m "Update Hauptprogramm V16"
git push

- Pruefen
git status
```
